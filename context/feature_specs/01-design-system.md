Read `AGENTS.md` before starting.

 We're adding the design system and UI primitve components

 Install and configure `shadcn/ui`.

 add these shadcn components:

 -Button
 -Card
 -Dialog
 -Input
 -Tabs
 -Textarea
 -ScrollArea  

 Do not modify the generated `components/ui/*` files after installation.

 Also Install `lucide-react`.

 Create `lib/utils.ts` with a reusable `c()` helper for merging Tailwind classes.

 Ensure all the components match the exisiting dark theme in `globals.css`.

 ### check when done

 -All components import without errors
 -`cn()` works properly
 -No default light styling appears